# Rename to .env and fill in your values

# Supabase → Settings → Database → Connection string (URI mode)
DATABASE_URL=postgresql://postgres:[TU-PASSWORD]@db.[TU-REF].supabase.co:5432/postgres

JWT_SECRET=cambia_esta_clave_por_una_muy_larga_y_segura
PORT=3000
